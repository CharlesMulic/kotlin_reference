package talks.bestpracunittesting

import org.junit.jupiter.api.Test

// can put method names inside `` in Kotlin

class TagClientTest {
    @Test
    fun `basic tag list`() {}

    @Test
    fun `empty tag list`() {}
}