
// in class file these are same method, sort(List)
// can change jvm name to keep name the same in source

@JvmName("sortStrings")
fun sort(strings: List<String>) {
    // ..
}

@JvmName("sortInts")
fun sort(ints: List<Int>) {
    // ..
}